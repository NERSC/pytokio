This file was generated by subsampling the Darshan logs generated on Cori on January 2, 2019:

    #!/bin/bash
    i=0
    for j in $DARSHAN_LOGPATH/2019/1/2/*.darshan
    do
        let i++
        if [ $((i%100)) == 0 ]
        then
            cp -v $j ~/tmp_darshanlogs/
        fi
    done

Then anonymizing them:

    mkdir $PYTOKIO_HOME/tests/darshan_corpus/
    j=0
    for i in ~/tmp_darshanlogs/*.darshan
    do
        darshan-convert --obfuscate --key 0 $i $PYTOKIO_HOME/tests/darshan_corpus/blah-$j.darshan
        let j++
    done

Then giving these new log files vaguely realistic names by running:

    $PYTOKIO_HOME/tests/tools/rename_darshanlogs.sh $PYTOKIO_HOME/tests/darshan_corpus/*.darshan

This step is necessary for the log indexer to store headers.username values.

Then the index database can be generated from these files:

    $PYTOKIO_HOME/bin/index_darshanlogs $PYTOKIO_HOME/tests/darshan_corpus --output $PYTOKIO_HOME/tests/inputs/darshanlogs.db
